@import url("../../css/common.css");

main {
    padding-top: 66px;
    min-height: 50rem;
}
.main-wrap {
    display: flex;
    flex-direction: column;
    gap: 4.8rem;
}
#title {
    padding-top: 2.4rem;
}
#title .title-wrap {
    display: flex;
    flex-direction: column;
    gap: 2.4rem;
}
.site-map {
    display: flex;
    align-items: center;
    align-self: flex-end;
}
.site-map a {
    display: flex;
    align-items: center;
    font-size: 1.2rem;
}
.site-map i.icon {
    display: inline-block;
    width: 1.6rem;
    height: 1.6rem;
    margin-right: 4px;
    background: url("../../image/icon/icon-home.png") 100% / cover;
}
#title {
    text-align: center;
}
#title h2 {
    font-size: 7rem;
    font-weight: bold;
    color: var(--color-blue);
}
#title .desc {
    font-size: 2.4rem;
    font-weight: 700;
    letter-spacing: -1px;
}
#faq #search-option {
    display: flex;
    gap: 1.2rem;
    justify-content: center;
    align-items: center;
}
.search-btn {
    height: 4rem;
    padding: 0 2.4rem;
    border-radius: 50px;
    font-size: 1.8rem;
    border: none;
    font-weight: 600;
    color: var(--font-medium);
}
.search-btn.active{
    background-color: var(--color-blue);
    color: white;
}
.search-btn:hover {
    opacity: 0.8;
}
#faq .faq-wrap {
    display: flex;
    flex-direction: column;
    gap: 4.8rem;
}
/* arcordian */
.accordion-item {
    border-left: none;
    border-right: none;
}
.accordion-button {
    height: 6.4rem;
    font-size: 1.5rem;
}
.accordion-button .cate {
    margin-right: 4px;
    font-weight: 600;
}
.accordion-button:not(.collapsed) {
    background-color: white;
}
.accordion-button:focus {
    border-color: transparent !important;
    box-shadow: none;
}
.accordion-body {
    font-size: 1.5rem;
    line-height: 1.2;
    padding: 2.4rem;
    background-color: var(--color-bg1);
}
